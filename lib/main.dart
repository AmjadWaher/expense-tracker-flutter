import 'package:expensetracker/start/start.dart';
import 'package:flutter/material.dart';

void main() {
  /*
  // Required to lock orientation
  WidgetsFlutterBinding();
  // Used to control the screen orientation
  SystemChrome.setPreferredOrientations([
    DeviceOrientation.portraitUp,
  ]).then((fn){});
  */
  runApp(const Start());
}
